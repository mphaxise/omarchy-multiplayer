# Decisions

Dated log. Each entry records the decision, the alternatives considered, the reason, and what would reopen it.

## 2026-09-01

**Shape: design-led prototype on the ARM rig.** Alternatives: working prototype first; research and upstream proposal only. Reason: the load-bearing claim (the session is the durable object, a terminal attaches to it) needs a running system to test, and the design work is what makes the test fair. Reopens if the rig cannot host the slice.

**First slice: one user, many agents.** Alternatives: agent to agent; user to user; user to agent control. Reason: testable on one VM with one identity, and the session object it produces is the dependency for the other three.

**Session engine: half-day OpenClaw spike before choosing.** Alternatives: adopt OpenClaw as given; Omarchy-native from the start. Reason: no evidence yet that the OpenClaw Gateway runs on aarch64 Arch in 8 GiB beside Hyprland. Decision rule is written in `PLAN.md`, Phase 2, before the spike starts.

**Repository: public from the first commit, MIT.** Reason: findings-style discipline from Omarchy-UX, and the outbound artifact is a proposal other Omarchy users can install or read.

**Dev seat: build and verify from the Mac over ssh into the VM.** Alternative: run Claude Code inside the Omarchy VM as the development seat. Reason: the guest's 8 GiB belongs to the sessions under test, the OpenClaw spike's free-memory rule needs a quiet guest, and ssh sidesteps the UTM synthetic-input quirks. Claude Code inside the VM is used for the baseline and walkthrough sessions only, because those sessions are evidence.


## 2026-09-01, evening, after the research phase

**Runtime: Herdr, through its socket API.** Alternatives: OpenClaw Gateway as the session engine; an Omarchy-native layer on tmux or abduco. Reason: Omarchy quattro already ships Herdr, bound to Super+Ctrl+Return, with a background server that survives window close, per-pane agent status detection, an events stream, and worktree support; building a second runtime beside it would duplicate what the distribution already installs. Fallback if Herdr is absent or broken on the ARM image: the x86-64 rig for one more half day, then a degraded mode on `git worktree` plus tmux with status limited to running or exited. Reopens if the Herdr spike (`setup/rig-questions.md`, items 2 to 10) fails on both rigs.

**OpenClaw: pattern source, install spike deferred.** The twelve catalog entries in `patterns/` carry its session, attribution, event, permission, and trust patterns into the spec. The Gateway install spike moves to the slice 2 gate, where a second person needs a gateway. Reopens if slice 2 starts.

**Session identity lives in Omarchy state.** Records under `~/.local/state/omarchy/sessions/<id>/`; Herdr's transient agent alias mirrors the session name. Reason: Herdr keeps no durable session identity beyond the harness's own resume id, and the experiment's object must outlive panes and reboots.

**Audience: AI-native product people who design and build.** Slice 1 adds a light closed-loop layer (`spec/09-closed-loop-surfaces.md`): goal template, registered preview, labeled captures, loop view, verdict. Reason: the essay this experiment rests on and the product-people landscape both point at a loop record no tool owns. Reopens if the closed-loop pass in evaluation shows the layer adds cost without legibility.

**Upstream framing corrected.** The morning plan called the upstream white space clean; two discussions (#5433, #8463) ask for parts of this with zero replies, and community plugins (Maestro, omarchy-hermes-sessions, Passpage) cover pieces. The outbound artifact acknowledges that lineage.

**Spec status: proposed.** Every file in `spec/` is proposed until the first rig session revises the session model. Nothing is live.

## 2026-09-02

**Runtime confirmed: Herdr.** The spike in `spikes/herdr-on-rig.md` met the decision rule from PLAN.md Phase 2: Herdr 0.8.2 is installed on the ARM image, its socket API lists panes and streams events to an external subscriber, `agent.start` launched Claude Code, metadata survives a config reload, and worktrees work. The Claude Code turn with `working`, `blocked`, and `done` transitions was observed after the in-VM login, so the rule is met in full. The degraded tmux fallback is retired.

**Wire format settled from the rig.** The core and the watcher now follow the observed protocol: `{"id", "method", "params"}` requests, `{"id", "result": {"type": ...}}` replies, `events.subscribe` with a `subscriptions` list of dotted types, underscored event names in the stream, and `session.snapshot` as the snapshot method. The full schema for protocol 20 is stored in `spikes/`.

**Review verdicts, afternoon.** From the `/ux-review` and `/design-qa` passes (`findings/evaluation-slice1-2026-09-02.md`): P1 blocks a public push; Option B, craft-first, for the open findings; stop-ship until the bar glyph and the state dots pass 3:1, because WCAG AA is the floor for anything carrying the Omarchy-UX method. All three are built (bad8373, cbac0e9). The feeling question the review asked (calm, control, or safety) stays open; the build treats control as the tie-breaker.

**The guest Claude stays plain.** The public design skill pack is installed in the VM under `~/.claude/skills/`; my private personal overlay is not, and anything that needs it runs from the Cowork seat on the Mac. Reason: private material stays out of the guest.

**Evaluation run 1 counts as evidence, not as the verdict.** The scenario ran over ssh without a stopwatch or a person; signals 2, 3, and 5 pass, 4 passes on one event, 1 is unmeasured (`findings/evaluation-run1-2026-09-02.md`). The verdict waits for a run that uses the panel's own keys and the stopwatch.

**The stopwatch is withdrawn from signal 1.** Late evening. I will not run the three five-second trials. Signal 1 is restated in `PLAN.md` as a capture-verified criterion: the waiting session is the first thing named on every surface (toast, badge, hero, first row), and runs 1 and 2 hold that evidence. Reason: one person with a stopwatch on a software-rendered VM measures the VM as much as the panel, and what the design controls is the cue chain, which the captures show. Consequence: the verdict rule in `spec/10` reads "measured by capture" for signal 1; the report says so.

**The rig stays on Omarchy 0b3f1b7.** `omarchy-update` ran on the aarch64 image at 20:22 and updated nine Arch packages, Claude Code among them (2.1.252 to 2.1.259 through mise), and nothing of Omarchy itself: `/usr/share/omarchy` is a root-owned git checkout no package owns, and `omarchy-update-dev` exits without pulling when the path is `/usr/share/omarchy`. The port's package repo carries an `omarchy` 4.0.1-2 package, which is its intended update path and is not installed. Nothing the plugin depends on changed upstream, so the evaluation stays consistent on one Omarchy commit with one harness bump, recorded here. Installing the package is a root action for a snapshot first, and the gap goes to the port maintainer.

**The panel closes itself to take a capture.** Late evening, run 4 (`findings/closed-loop.md`). Feedback typed into a row's Send field while a preview is registered goes out as `send --with-capture`, and the capture is of the preview window's geometry, which the panel overlay would cover. So the panel closes, the command starts 400 ms later, a failure nobody can see goes out as a toast, and the row's loop count is the evidence of delivery. The alternative, capturing with the panel in the picture, would hand the agent an image with a session list over the thing the feedback is about. The legend caps at six entries, esc first to go, because "p preview" as a seventh elided "→ more".

**The closed loop is verified from a script, and the sitting is Praneet's.** Runs 3 and 4 exercised every spec/09 surface the panel exposes, with pre-written instructions and, in run 4, `wtype` pressing the keys. That verifies the mechanism and nothing about a designer's judgment. The one sitting spec/09 section 8 asks for stays open until Praneet runs it at the rig; no script will stand in for it.

**Phase 6 is drafted, and every outbound choice stays Praneet's.** Late evening. `outbound/` holds the marketplace listing package (README, install and uninstall scripts, preview, a build script that assembles the listing repository and that `omarchy plugin validate` accepts on the rig), the submission text in the marketplace's own form, a discussion post for omacom/omarchy, and one issue text per upstream candidate. Both the listing and the post are drafted so the choice between them is made on the texts. The plugin id is left as built, `praneet.agent-sessions`, with the build able to apply `io.github.mphaxise.agent-sessions`, because the marketplace makes ids permanent and a rename touches the plugin directory, the IPC target, and the bindings on every machine that has it. Nothing is created or posted on GitHub until he approves the exact text and target.

**The plugin is Keepalive.** Late evening, Praneet's pick from five names (Keepalive, Still Running, Standing Agents, Agent Keep, Long Run). "Agent Sessions" named the object; Keepalive names the promise, coding agents that stay running on the desktop after the terminal is gone. The listing id is `io.github.mphaxise.keepalive`, the listing repository `mphaxise/omarchy-keepalive`; the commands stay `omarchy-agent-session-*` because they describe the object. The dev checkout and the rig keep `praneet.agent-sessions` until the rig runs one session under the listing id, a rename that touches the rig's bindings and waits for a go.

**The rig and the dev checkout carry the listing id.** 22:27, on Praneet's "let's go for it" after reading plugins.omarchy.org's guides. `praneet.agent-sessions` is retired (its directory moved to `~/Work/plugins-retired/` on the rig), `io.github.mphaxise.keepalive` is installed from the built listing directory, the two keybindings in `~/.config/hypr/bindings.lua` point at it, and the repository's plugin directory is renamed to match. The marketplace's pre-share checklist ran under the new id (run 6): validate, qmllint (exit 0, the same warning classes as the built-in clock and agents plugins), shell restart, `shell summon` and `hide`, our IPC, Escape, disable, re-enable, removal and re-add, one session started from the panel. Publishing (the repository, the submission issue, the discussion post) stays gated on his word per step, per PLAN.md Phase 6 as revised.

**A Herdr restart orphans; it never ends.** 22:30, from Praneet's reboot: the reconciler read the fresh shells Herdr restores after a restart as harnesses that had exited, marked his two working sessions failed one second after the server came back, and Enter opened receipts. The rule now: a Herdr server that started after the session's binding (its socket's mtime, two-second margin) explains the vanish, and the session is orphaned, revivable, at normal urgency. Sessions ended that way before the rule existed keep their receipt and revive on Enter anyway, because nobody decided they were over: the panel labels them "resumes conversation" with Revive and Receipt, and `open` records failed → orphaned → working. Verified on the rig on a run-2 session; his two are waiting for his Enter.

**Phase 6 published, on "go all four".** 22:45. The dev repository pushed (17 commits), `mphaxise/omarchy-keepalive` created and pushed from the built listing, the marketplace submission opened as issue #4566, and the discussion posted to omacom/omarchy as #9936 in Show and tell. "Two days" stays as written; the aarch64 image's maintainer is named, Gabriel Galán, from the GitHub profile behind `ggalancs/omarchy-arm-utm`. Before the push, the run-6 captures were cropped to the panel and the bar because the full screens carried my own sessions' terminals. Before the submission, the rig removed its copy of the plugin and installed it from the public URL with `omarchy plugin add`, which is the path a stranger takes.

**Plan v3.** Late night, on Praneet's ask for the full program plan and his first direction for it: one person pulling two agents into a session, each with its own task. `PLAN.md` v3 keeps v2's question and facts, records what slice 1 settled, and plans slices 2 to 4 as bounded tests (question, success signals, limit, review point), ordered by learning value and reversibility. Slice 2 opens with agent lanes on the existing parent-and-child record, then the designer's measured sitting, then the second harness and the x86 rig, then watchers. Slice 3 is the second person, identity space decided on a proxy first. Slice 4 is structured fan-out and the upstream path. Ten decisions are his, each with a proposed default so work can start.

**Keepalive is a build output; main ships; slices branch.** Late night, from Praneet's question about maintaining Keepalive while slice 2 proceeds. One source repository, `omarchy-multiplayer`, holds everything; the listing repository is assembled from it by `outbound/build-listing.sh`, which now keeps the listing's git history and writes a `SOURCE` file naming the dev commit. `main` is shippable at all times; slice work goes on `slice-N/<topic>` branches and merges when its signals pass. `outbound/release-keepalive.sh <version>` cuts a release (tests, manifest bump, tag `keepalive-v<version>`, one commit on the listing clone) and stops before anything is pushed. 0.1.0 is tagged at `ea2bc08`, the commit the published listing `b96ecbc` reproduces byte for byte apart from `SOURCE`. Feedback path: reproduce on the rig, fix on `main` with a test, release.

**Slices 2 and 3 built ahead of the rig; branches hold them.** 2026-09-03, early morning, from Praneet's "go until slice 3". The loop built lanes, watchers, the artifact leftovers, and the two-people mechanics with 162 tests, on `slice-2/lanes` and `slice-3/two-people`, and stopped when the next useful action needed the rig, which was off (a `utmctl start` from the Mac timed out and the VM stayed stopped; I leave the VM alone from here). Two things settled on the way: a lane branch sits beside the session branch as `session/<name>--<lane>`, because git refs cannot nest under an existing branch; and slice 3 runs first as a proxy on one OS user (`OMARCHY_ACTOR`, or the ssh client host), which tests the mechanics and claims nothing about isolation. `main` stays at Keepalive 0.1.0 until run 9 passes with captures.

**Run 9 holds; lanes are on main; 0.2.0 is built and waits.** 2026-09-03, 08:38 to 08:50, on Praneet's "try now" after he brought the VM up. Two Claude Code lanes on one goal held all five slice-2a signals with captures (`findings/evaluation-run9-lanes-2026-09-03.md`); three fixes landed during the run (lane lines need a length test, not `Array.isArray`, on a QVariantList; `w` walks lanes because `l` is an arrow in the shell's key catcher; watch notices only for material events, because every state flip had started a turn on the parent). `slice-2/lanes` merged into `main` (`1b57053`) under the branch rule, and `outbound/release-keepalive.sh 0.2.0` tagged `160f86f` and built one commit onto the listing clone (`06252f5`). The pushes and the marketplace's newer-commit form wait for his word.

**The second identity space is `human:<user>@<host>` from the ssh client, with an override.** 2026-09-03, 09:00 to 09:17, run 10 (`findings/evaluation-run10-two-people-2026-09-03.md`). The proxy PLAN.md names first, the Mac over ssh, worked for every mechanic slice 3 adds: visibility, access, a suggestion that waits and runs under its author's name once the owner presses `y`, dismissal, assignment that moves the toast's owner and nothing else, presence outside the record. What it exposed is the name: the rig resolves the Mac to `_gateway`, so the second person is "omarchy@_gateway" everywhere, and with both people called "omarchy" the surfaces needed a rule (label, or `user@host` when the label would read as your own). The next identity step is a second OS account on the same machine, where the user part differs and the trust boundary is real; verified profiles stay reserved. Two more things settled: the reconciler's sweep matches panes by the session id the core stamps on them, never by workspace id, because Herdr reuses workspace ids after a restart and the sweep had closed a new session's workspace five seconds after creation; and `send` to a blocked agent stays refused, which leaves a second person on another host unable to answer an agent's question, a design question for the sitting. `slice-3/two-people` merges into `main` on this evidence; the sitting with a second person, which is the judgment half of slice 3, is Praneet's to schedule.

**History is kept and reachable; the panel shows a day; a stop resumes.** 2026-09-03, 14:00 to 14:20, from Praneet's question after starting the VM: where are yesterday's agents, and shouldn't a persistent agent stay available for a couple of weeks. The record already did: 52 session directories on the rig, none deleted, every one with a receipt, its events, and a harness resume id. What hid them was the panel, and one rule in the model. The panel shows ended sessions for 24 hours and then "the receipt is their record", with no surface for a receipt older than a day but the CLI; his six from the previous evening sat at rows 12 to 18 behind 13 evaluation sessions from the morning, and would have left the panel entirely at 22:12. And `open` refused a `stopped` session (exit 5) even with the transcript on disk, so "persistent" meant the record and not the conversation once a person had pressed x. Three decisions. Retention is a property of a surface, never of a record: nothing ages out, a `prune` command is the only deletion the model will ever add, and it is not built (`01-session-model.md`, Retention). The panel asks for its window (`list --ended-within 24h --json`), which also defuses a bug that was one evaluation run away: the payload carried every record ever, 53 KB against the panel's 64 KB cap, past which the panel shows an error row and no sessions; the payload now says how many ended earlier, the panel's footer names that count and `e`, which opens `history` (fourteen days grouped by day, in a TUI window like the receipt), the bar icon stays once any record exists instead of hiding with the last live session, and the empty state points at history. A `stopped` session with a transcript resumes on `open` the way an orphan does, stopped → orphaned → working with the detail "resumed after a stop", and its row reads ⏎ Resume; a `done` closed with a verdict stays closed, because a verdict is a decision and a stop is a pause. Alternatives considered: a longer panel window (the panel is a bar popup, and 34 rows in it is the problem the collapse was added for); splitting stop into pause and stop (a new state for a distinction the resume rule already makes); deleting old records (nothing here is large, and the receipt is the evidence the sittings will need). Built on `slice-4/history`; reopens if the sitting says the panel needs more than a day in view, or if resuming a stopped Claude Code session turns out to lose the turn in flight in a way the receipt cannot explain. Run 12 held on the rig with captures (`findings/evaluation-run12-history-2026-09-03.md`): the window, History, and a resume of run 11's stopped session with the same harness ref. Merged into `main` (`1d99bf0`) under the branch rule at 14:45; unpushed, and no release cut, both Praneet's word.

**Pause is a state; stop stays resumable; prune is explicit.** 2026-09-03, 15:00, from Praneet's next question once history was in: do we need a pause, and shouldn't everything a person has not killed stay, forever, until cleaned up. Yes to all three, and the model now says so. `paused` is a live session with no process: `z` on a live or orphaned row, one press because it is reversible, exits the harness, closes the pane, writes a checkpoint receipt, leaves the worktree, and parks the record in a Paused section of the panel that has no window; a reboot leaves it there, and Enter resumes it on its transcript. It differs from `idle`, which holds a harness process in Herdr with its memory, and from `stop`, which ends the session, runs the worktree cleanup, and sends the row to History after a day. Stop keeps two presses and keeps Resume: pause is the intent to return, stop the intent to end, and the transcript stays a safety net over the one Praneet hit that afternoon. `prune` is the only deletion, built now rather than reserved: ended sessions older than a duration, or one named session, a dry run unless `--yes`, never scheduled, never a paused session, `--older-than` under a day refused. Alternatives considered: pause as a flag on stop (the person's intent is the thing that differs, so it is a state); pause keeping the process alive (that is `idle`, and the process is the cost); making stop final now that pause exists (clean, and it would have cost him his six sessions again); a scheduled prune (nothing here is large, and the receipts are the evidence). Built on `slice-4/pause`; reopens if the sitting says two ways back confuse more than they save, or if the Paused section grows past what a bar panel can hold.
