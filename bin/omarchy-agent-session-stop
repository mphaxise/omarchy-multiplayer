#!/bin/bash

# omarchy:summary=Stop a session and, by default, its children
# omarchy:args=<id-or-name> [--keep-children] [--reason <text>]
# omarchy:examples=omarchy agent session stop api-refactor --keep-children

set -euo pipefail

exec omarchy-agent-session-core stop "$@"
