#!/bin/bash

# omarchy:summary=Rename a session
# omarchy:args=<id-or-name> <new-name>
# omarchy:examples=omarchy agent session rename api-refactor api-refactor-2

set -euo pipefail

exec omarchy-agent-session-core rename "$@"
